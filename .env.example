# Copy to .env.local (gitignored) and fill in once the gallery S3 bucket +
# CloudFront distribution exist. Points the gallery grid/lightbox at real photos
# during local dev instead of 404ing.
VITE_GALLERY_BASE_URL=https://xxxxxxxxxxxxxx.cloudfront.net
